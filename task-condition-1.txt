Задача 1
  Реализуйте функцию palmTree(n), которая принмает целое положительное число и 
  возвращает строку, состоящую из символов "p". Например palmTree(3) вернет "ppp"
  Примечание при нечисловом аргументе функция должна вернуть false, а при вызове
  без аргумента один символ "p"
  (решение в одну строку !)

Задача 2
  Напишите однострочное решение, которое вычисляет сумму квадратных корней для всех 
  чётных чисел целочисленного массива, точность 2знака после запятой


Задача 3
  Напишите функцию, которая пишет в консоль число в заданном диапазоне, 
  в случае, если оно успешно делится или не делится с остатком или без 
  остатка в зависимости от параметров.


Задача 4 
  Написать функцию (в одну строку с проверкой на строку) которая принимает строку 
  и возвращает каждое новое слово с большой буквы


Задача 5 
  Напишите функцию change_register(str), которая принимает в качестве аргумента строку и и заменяет регистр 
  нечетного элемента на верхний (использовать проверку на строку, код должен быть в одну строку)
  "каждый охотник желает знать" =>  "КаЖдЫй ОхОтНиК ЖеЛаЕт ЗнАтЬ"


Задача 6
  Напишите функцию change_register(str), которая принимает в качестве аргумента строку 
  и заменяет регистр каждого символа на противоположный. 
  Например, если вводится «КаЖдЫй ОхОтНиК», то на выходе должно быть «кАжДыЙ оХоТнИк».
  (использовать проверку на строку, код должен быть в одну строку)


Задача 7
  Напишите функцию insensitive_search(str1, str2), которая осуществляет поиск подстроки str2 в строкеstr1 
  без учёта регистра символов. (использовать проверку на строку, код должен быть в одну строку)
  // console.log(insensitive_search('Изучаю JavaScript', 'javascript')) //true
  // console.log(insensitive_search('Изучаю JavaScript', 'javascriptS')) //false


Задача 8
  Напишите функцию initCap(str), которая преобразует стиль написания составных слов строки в CamelCase, 
  при котором несколько слов пишутся слитно без пробелов, 
  при этом каждое слово внутри строки пишется с заглавной буквы.
  (использовать проверку на строку, код должен быть в одну строку)
  "hEllo woRld" => HelloWorld


Задача 9
  Создайте метод объекта String endsWith(), который сравнивает подстроку str1 с окончанием исходной строки 
  str и определяет заканчивается ли строка символами подстроки.

  const str = "Каждый охотник желает знать"; 
  const str1 = "скрипт";
  const str2 = "знать";
  console.log(str.endsWith(str1)) //false
  console.log(str.endsWith(str2)) //true
  
  
Задача 10
  Создать функцию multiplicationTable(row,col) которая будет строить матрицу
  (Код должен быть в одну строку)
